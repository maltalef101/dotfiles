import * as React from "react";
import { Link } from 'gatsby';

import "./navbar.css";

const Navbar = ({ name, to, children }) => {
  return (
    <nav className="flex-initial flex-row">
      {children}
    </nav>
  )
};

export default Navbar;
