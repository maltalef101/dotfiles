#! /bin/sh

touchId="$(xinput | grep -i "touchpad" | awk '{print $6}')"

touchStatus=$(xinput list-props $touchId | grep "Device Enabled" | awk '{print $4}')

[ "$touchStatus" = "1" ] && xinput disable $touchId
[ "$touchStatus" = "0" ] && xinput enable $touchId
